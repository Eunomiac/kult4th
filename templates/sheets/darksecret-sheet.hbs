{{log this}}
{{log "DATA" data}}

<form class="kult4th-form {{cssClass}}" autocomplete="off">
		<header class="sheet-header">
			<span class="header-bullet">•</span>
			<span class="header-name">{{item.name}}</span>
			<span class="header-filler"></span>
		</header>
	<main class="kult-text-container tab-content">
		{{#unless (areEmpty item.tData.rules.intro item.tData.rules.outro)}}
		<p class="rules-top">
			{{~#if item.tData.rules.intro}}<span class="text-intro">{{{formatForKult item.tData.rules.intro}}}</span>{{/if}}
			{{~#if item.tData.rules.outro}} <span class="text-outro">{{{formatForKult item.tData.rules.outro}}}</span>{{/if}}
		</p>
		{{/unless}}

		{{~#if item.tData.rules.listRefs}}
		{{~#with item.tData.lists as |LISTS|}}
		{{~#each ../item.tData.rules.listRefs as |listKey|}}
		{{~#with (lookup LISTS listKey) as |listData|}}
		<h2 class="list-name">{{listData.name}}</h2>
		{{~#if listData.intro}}<span class="list-intro">{{{formatForKult listData.intro}}}</span>{{/if}}
		<ul class="rules-list list-{{listKey}}">
			{{#each listData.items as |item|}}
			<li>{{{formatForKult item}}}</li>
			{{/each}}
		</ul>
		{{/with}}
		{{/each}}
		{{/with}}
		{{/if}}
	</main>
</form>